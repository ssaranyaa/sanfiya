#include<stdio.h>
#include<string.h>
int main()
{
char text[400];
int length;
printf("\n enter the string");
scanf("%d",&text);
len+str len(text);
printf("\n the length of the string is:"len);
return 0;
}
