#include<stdio.h>
#include<string.h>
void main()
{
char a[i];
int i,k;
printf("\n enter the string:");
scaf("%s",&a[i]);
printf("\n enter the limit");
scaf("%d",&k);
for(i=0;i<k;i++)
{
printf("\n",a[i]);
}
return 0;
}
